import React from 'react';
import Error from './index';
import stores from '../../../tests/utils/fakeStores';

describe("Error", () => {
  it("should render", () => {
    expect(Error).toBeDefined()
  });
});